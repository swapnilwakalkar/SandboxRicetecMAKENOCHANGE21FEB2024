public class customPathStatusCtrl {/*
 
     @AuraEnabled
    public static Leads__c stageNamePath(String recId){
        Leads__c statusList = [SELECT Id, Name, Lead_Status__c FROM Leads__c WHERE Id=:recId];
        system.debug('statusList ' + statusList);
        RETURN statusList;
    }
 */
}