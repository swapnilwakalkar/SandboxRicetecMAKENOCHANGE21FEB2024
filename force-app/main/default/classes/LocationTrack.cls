public with sharing class LocationTrack {/*
    @AuraEnabled(cacheable=true)
    public static String getDayPlannerRecordId() {
        // Query the Day_Planner__c record and retrieve the record ID
        Day_Planner__c dayPlanner = [SELECT Id FROM Day_Planner__c LIMIT 1];
        return dayPlanner.Id;
    }*/
}